<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ruf.view.locationmap">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <meta-data android:name="OnlineMaps.Key" android:value="@string/key_maps_api" />
        <meta-data android:name="OnlineSearch.Key" android:value="@string/key_maps_api" />
        <meta-data android:name="OnlineTraffic.Key" android:value="@string/key_maps_api" />
        <meta-data android:name="OnlineRouting.Key" android:value="@string/key_maps_api" />

        <activity
                android:name=".MapsActivity"
                android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>